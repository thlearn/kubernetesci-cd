pipeline{
agent none

  parameters{
   string(name: 'imageName', defaultValue: 'test')
  }
  try{
  
  stage('compile'){
        echo $imageName
        
       }
  
 }catch (err) {
      currentBuild.result = 'FAILURE'
    }
}

