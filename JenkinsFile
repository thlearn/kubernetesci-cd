node{
  def Namespace = "default"
  def ImageName = "sayarapp/sayarapp"
  def Creds = "2dfd9d0d-a300-49ee-aaaf-0a3efcaa5279"
  try{
  stage('Checkout'){
      checkout scm
     
      }
  stage('compile'){
  
        sh "echo $JAVA_HOME"
        sh "echo $MAVEN_HOME"
        sh "/var/jenkins_home/apache-maven-3.6.1/bin/mvn clean install"
      }
  stage('Docker Build, Push'){   
             withDockerRegistry(credentialsId: 'docker', url: 'https://registry.gitlab.com') {
             
             //sh "docker build -t  registry.gitlab.com/hareshgitlab/mydockerregistry/mytomcat:1.0 ."
             //sh "docker push  registry.gitlab.com/hareshgitlab/mydockerregistry/mytomcat:1.0 "
             
             }
      
      }
      
  stage("Deploy to Kubernetes"){
      sh "kubectl create -f k8s/deployment.yml"
      sh "kubectl create -f k8s/nodeportsvc.yml"
  }
     } catch (err) {
      currentBuild.result = 'FAILURE'
    }
}
