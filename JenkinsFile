node{
  
  try{
  stage('Checkout'){
      checkout scm
     
      }
  stage('compile'){
  
        sh "echo $JAVA_HOME"
        sh "echo $MAVEN_HOME"
        
        
      }
      stage('compile'){
      withDockerServer([uri: 'tcp://10.0.103.101:2375']) {
       sh "docker ps"
     }
     }
  
     } catch (err) {
      currentBuild.result = 'FAILURE'
    }
}
