node{
  parameters{
   string(name: 'imageName', defaultValue: 'test')
  }
  try{
  stage('Checkout'){
      checkout scm
        }
  stage('compile'){
        echo $imageName
        
       }
  
 }catch (err) {
      currentBuild.result = 'FAILURE'
    }
}

